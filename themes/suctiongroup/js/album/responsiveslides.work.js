// JavaScript Document

// You can also use "$(window).load(function() {"
    $(function () {

      // Slideshow 1
      $("#slider1").responsiveSlides({
        auto: true,
        pager: true,
        nav: false,
        speed: 1000,
        manualControls: '#slider1-pager',
      });
     
    });